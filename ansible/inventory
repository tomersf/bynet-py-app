[jenkinsserver]
jenkins

[testserver]
test

[prodserver]
prod

[vms:children]
jenkinsserver
testserver
prodserver

[vms:vars]
ansible_host = 'REMOTE_IP'
ansible_ssh_user = cloud_user
ansible_ssh_private_key_file = ~/.ssh/id_rsa
